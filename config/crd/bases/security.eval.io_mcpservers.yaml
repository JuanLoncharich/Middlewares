---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: mcpservers.security.eval.io
spec:
  group: security.eval.io
  names:
    kind: MCPServer
    listKind: MCPServerList
    plural: mcpservers
    shortNames:
    - mcps
    singular: mcpserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.repositoryUrl
      name: Repository
      type: string
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .spec.active
      name: Active
      type: boolean
    - jsonPath: .status.overallRiskStatus
      name: Risk
      type: string
    - jsonPath: .status.lastRunRef
      name: LastRun
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MCPServer is the Schema for the mcpservers API.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: MCPServerSpec defines the desired state of MCPServer.
            properties:
              active:
                default: true
                type: boolean
              agentSuite:
                items:
                  description: AgentReference names an OpenCodeAgent in the same namespace.
                  properties:
                    name:
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
              credentialsSecretRef:
                type: string
              path:
                type: string
              ref:
                default: main
                type: string
              repositoryUrl:
                pattern: ^https://
                type: string
              runHistoryLimit:
                default: 5
                format: int32
                minimum: 0
                type: integer
              schedule:
                minLength: 9
                type: string
              targetPort:
                default: 8080
                format: int32
                maximum: 65535
                minimum: 1024
                type: integer
              timeoutSeconds:
                default: 900
                format: int32
                minimum: 60
                type: integer
              transport:
                default: stdio
                enum:
                - stdio
                - sse
                type: string
            required:
            - agentSuite
            - repositoryUrl
            - schedule
            type: object
          status:
            description: MCPServerStatus defines the observed state of MCPServer.
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastEvaluationDate:
                format: date-time
                type: string
              lastFinalScore:
                format: int32
                type: integer
              lastRunRef:
                type: string
              nextScheduledTime:
                format: date-time
                type: string
              overallRiskStatus:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
